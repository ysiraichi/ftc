INCLUDE_DIRECTORIES(.)

FLEX_TARGET(Scan LexFTiger.l ${CMAKE_CURRENT_BINARY_DIR}/LexFTiger.c)

ADD_EXECUTABLE(ftc ${FLEX_Scan_OUTPUTS})
TARGET_LINK_LIBRARIES(ftc ${FLEX_LIBRARIES})
INSTALL(TARGETS ftc RUNTIME DESTINATION bin)
